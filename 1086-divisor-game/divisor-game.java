class Solution {
    public boolean divisorGame(int n) {
        boolean dp[]=new boolean[n];
        // if(n==2){
        //     return true;
        // }

        if(n%2==0)return true;

        // for(int i=0;i<n;i++){

        // }

        return false;

    }
}